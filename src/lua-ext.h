#ifndef LUA_EXT_H
#define LUA_EXT_H

/*

class boost::asio::io_service;

void lua_write_string(boost::asio::io_service *io_service, char *s, size_t len);

#define lua_writestring(s,l)   (lua_write_string(L->io_service, s, l))
#define lua_writeline()        (lua_write_string(L->io_service, "\n", 1))

*/

#endif /* end of include guard: LUA_EXT_H */
